sudo docker run -it --rm --name=hexoblog \
-e HEXO_SERVER_PORT=4000 \
-e GIT_USER="wsx" \
-e GIT_EMAIL="246553278@qq.com" \
-v /src/blog:/app \
-p 4000:4000 \
hexo

sudo docker build -t hexo .


sudo docker logs hexoblog -f

sudo docker rm -f hexoblog